import React from 'react';
import { SearchBoxProps } from '../../../typescheck/interfaces';

const SearchBox: React.FC<SearchBoxProps> = ({onSearchChange }) => {
    return (
        <div className="" >
           
        </div >
    );
}   
export default SearchBox;